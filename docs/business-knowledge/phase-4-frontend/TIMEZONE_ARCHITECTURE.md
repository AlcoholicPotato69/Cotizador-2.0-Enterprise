# TIMEZONE ARCHITECTURE (Fase 4.2)\n\n## 1. Persistencia Inmutable (Backend)\n- Todo \`datetime\` guardado en PocketBase se almacena obligatoriamente en **UTC Absoluto** (\`YYYY-MM-DDTHH:mm:ss.sssZ\`).\n- Está estrictamente prohibido guardar fechas con offset local en la base de datos para prevenir corrupción si el servidor cambia de región.\n\n## 2. Conversión Frontend (UI)\n- El Frontend nunca asume \`UTC-6\` permanentemente.\n- El \`tenantStore\` expondrá un \`timezone\` configurable por Inquilino (ej. Plaza Mayor: \`America/Mexico_City\`).\n- Cuando los componentes (\`DsTable\`, \`DsDatePicker\`) lean el \`datetime\` UTC desde PocketBase, utilizarán la API de internalización de JS (\`Intl.DateTimeFormat\`) o librerías *lightweight* como \`date-fns-tz\` para convertir el UTC al Timezone oficial del Tenant activo.\n- Al enviar datos hacia el backend, el \`DsDatePicker\` convierte la selección local nuevamente a UTC absoluto antes de disparar el POST/PATCH.`,
    'DOCUMENT_WORKSPACE_ARCHITECTURE.md': `# DOCUMENT WORKSPACE ARCHITECTURE\n\nEl \`DsDocumentViewer\` es el ecosistema documental único de la plataforma.\n\n## 1. Distribución Visual\n- **Visor Central (70%)**: Módulo adaptable que detecta el MIME Type y carga el renderizador adecuado (PDF.js, XML Viewer con syntax highlighting, o \`<img>\` para evidencias).\n- **Panel Lateral (30%)**: Dos pestañas maestras (Metadata y Auditoría).\n\n## 2. Metadata Panel\n- Expone el contexto criptográfico e inmutable: Tenant, Cliente, RFC, UUID, Status, Fecha, Hash SHA-256.\n- Incluye indicador de retención (Legal Hold).\n\n## 3. Audit Timeline\n- Consume la colección de auditoría para pintar la historia del archivo: quién lo subió, quién lo previsualizó, quién le dio *Aprobar* (Four-Eyes Principle).\n\n## 4. Seguridad Activa\n- Las URLs fuente inyectadas en el Visor Central caducan por tiempo.\n- El lienzo inyecta marcas de agua (*Watermarks*) con el \`user_id\` y \`ip\` del operador actual sobre el PDF/Image.
